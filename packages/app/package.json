{
  "name": "simple-voice-recorder-app",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "sync": "yarn install && npx cap sync",
    "dev:android": "export STAGE=dev && yarn sync && npx cap open android",
    "dev:ios": "export STAGE=dev && yarn sync && npx cap open ios",
    "prod:android": "export STAGE=prod && yarn sync && npx cap open android",
    "prod:ios": "export STAGE=prod && yarn sync && npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^4.4.0"
  },
  "dependencies": {
    "@capacitor/android": "^4.4.0",
    "@capacitor/core": "^4.4.0",
    "@capacitor/ios": "^4.4.0",
    "capacitor-voice-recorder": "^4.0.0"
  }
}